.countdown.countdown-container
  .clock.row
    .clock-item.clock-years.countdown-time-value.w50
      .wrap
        .inner
          #canvas-years.clock-canvas
          .text
            p.val
              | 0

    .clock-item.clock-months.countdown-time-value.w50
      .wrap
        .inner
          #canvas-months.clock-canvas
          .text
            p.val
              | 0

    .clock-item.clock-days.countdown-time-value.w50
      .wrap
        .inner
          #canvas-days.clock-canvas
          .text
            p.val
              | 0

    .clock-item.clock-hours.countdown-time-value.w50
      .wrap
        .inner
          #canvas-hours.clock-canvas
          .text
            p.val
              | 0

    .clock-item.clock-minutes.countdown-time-value.w50
      .wrap
        .inner
          #canvas-minutes.clock-canvas
          .text
            p.val
              | 0

    .clock-item.clock-seconds.countdown-time-value.w50
      .wrap
        .inner
          #canvas-seconds.clock-canvas
          .text
            p.val
              | 0